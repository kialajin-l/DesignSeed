/**
 * DesignSeed — 统一导出
 */

const renderer = require('./renderer');
const styles = require('./templates/styles');
const components = require('./components/ui-components');

module.exports = { renderer, styles, components };
